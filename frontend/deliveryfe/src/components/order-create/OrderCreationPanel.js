import React from "react";
import { Card } from "antd";

// TODO: Implementation owned by teammate A
const OrderCreationPanel = ({ onOrderCreated }) => <Card />;

OrderCreationPanel.defaultProps = {
  onOrderCreated: () => {},
};

export default OrderCreationPanel;
